    <%= link_to "create Instructor", new_instructor_path(@instructor) %>

<% @instructor.each do |i| %>
<div id="delete<%= i.id%>"> 
<div class="container">
<table style= "width: 100%">
<%= link_to "Edit", edit_instructor_path(i.id) %><br>
<%= link_to "Delete", instructor_path(i.id), method: :delete, remote: true %>
    <tr>
        <th>Instructor ID</th>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Username</th>
        <th>Birthday</th>
        <th>Highest Education Achieved</th>
    </tr>
    <tr>
        <td><%= i.id %></td>
        <td><%= i.first_name %></td>
        <td><%= i.last_name %></td>
        <td><%= i.username %></td>
        <td><%= i.birth_date %></td>
        <td><%= i.highest_completed_education %></td>
    </tr>
</table>
</div>
</div>

<% end %>

